name: AJ compatibility
on: [push, pull_request, workflow_dispatch]
permissions:
  contents: read
jobs:
  kit:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python: ['3.10', '3.14']
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - run: python -m unittest discover -s tests/ai_kit -v
      - run: python tools/ai-kit/scripts/bootstrap.py validate .
      - run: python tools/ai-kit/scripts/bootstrap.py doctor .
